<?php

/*
 * Из полного конфига Livewire здесь задан только раздел временных загрузок:
 * остальные ключи пакет подставляет сам (mergeConfigFrom сливает значения
 * верхнего уровня, поэтому раздел приходится указывать целиком).
 *
 * Смысл правки — потолок размера файла. У Livewire он по умолчанию 12 МБ,
 * и фотография с камеры (20-30 МБ) отсекалась ещё до нашей валидации.
 * Обычно до сервера доезжает уменьшенная в браузере копия (см. настройку
 * FileUpload в AppServiceProvider), но запас нужен на случай, когда браузер
 * уменьшить не смог — иначе загрузка снова обрывается без внятной ошибки.
 */

return [

    'temporary_file_upload' => [
        'disk' => null,
        'rules' => ['required', 'file', 'max:40960'], // 40 МБ
        'directory' => null,
        'middleware' => null,
        'preview_mimes' => [
            'png', 'gif', 'bmp', 'svg', 'wav', 'mp4',
            'mov', 'avi', 'wmv', 'mp3', 'm4a',
            'jpg', 'jpeg', 'mpga', 'webp', 'wma',
        ],
        'max_upload_time' => 5,
        'cleanup' => true,
    ],

];
